# alpha-pon 設計・欠陥台帳・実装ロードマップ — 2026-09-10

作成: 2026-09-10 JST / 基準: `main` = `d65048b0` (PR #2017)
ゴール: エッジを検証し、通知を受け、買って、儲ける

この文書が現行の正本。`ROADMAP.md` / `TODO.md` は実態と乖離しているため置換する。
数値・仕様はすべてコードを読むか実行して確認した。未確認は「要確認」と明示する。

---

## 0. 現在地

### 0-1. 既にあるもの（品質が高い。作り直さない）

| 部品 | ファイル | 状態 |
| --- | --- | --- |
| PIT Price Store | `src/research/price-store.ts` (785行) | 完成・未使用 |
| Backtest エンジン | `src/research/backtest.ts` | 完成・fixtureのみ |
| コスト / Net Alpha / FDR | `src/research/net-alpha.ts` | 完成 |
| J-Quants Free Provider | `src/research/providers/jquants-free.ts` | 完成・未実行 |
| 価格取得CLI / 検証CLI | `src/research/cli/` | 完成・未実行 |
| Edge 定義 | `research/edge_registry/edges/*.yml` 3本 | 完成・Sample 0 |
| Security Master | `src/research/security-master.ts` (974行) | 完成・未接続 |
| ロックアップ抽出 | `src/extract-lockup-events.ts` | 既存 |
| 決算日推定 | `src/estimate-first-earnings.ts` | 既存 |
| 関連銘柄DB | `config/company-network.yml` | サンリオのみ |

**問題は「核が無い」ではなく「核に一度もデータが流れていない」。**

### 0-2. 2026-09-10 に実装したもの

| ファイル | 内容 |
| --- | --- |
| `src/research/signals/earnings-gap.ts` | 決算ギャップ Signal Generator |
| `src/research/signals/backtest-bundle.ts` | Bundle Builder |
| `scripts/build-earnings-gap-fixture.ts` | 合成 fixture 生成 |
| テスト3本 41ケース | 変異テストで実効性を確認済み |

合成データで end-to-end が通った（signal生成 → bundle → backtest → Net Alpha → FDR）。
実装中に**株式分割が -50% の偽シグナルになる欠陥**を発見し、2段構えのガードで修正した。

---

## 1. データ制約（コードから確認済み）

`JQUANTS_FREE_ENTITLEMENT`（`verifiedAt: 2026-08-07`）:

```
delayDays:             84      12週間の遅延
historyWindowYears:    2       履歴は2年のみ
topix:                 false   TOPIX直接不可 → 1306 ETF で代用
indices:               false
stockOhlc:             true
redistributionAllowed: false   research/prices/ は .gitignore 済み
```

rate limit 既定 3000ms/req（500銘柄の全履歴で約25分）。

**帰結**

1. **J-Quants Free では実売買も紙トレードもできない。** 84日遅延。**検証専用**
2. benchmark は 1306 ETF 代用。信託報酬分の乖離を計測して記録する
3. backtest 期間は最大2年
4. TDnet public viewer は**約1ヶ月しか遡れない**（実測: 2026-06-10 / 2025-09-10 とも
   `first page not found`。2026-09-09 は127件取得可）

---

## 2. 欠陥台帳

### 2-1. 統計・測定の欠陥（**これが最優先。放置すると全数字が嘘になる**）

| # | 欠陥 | 影響 | 状態 |
| --- | --- | --- | --- |
| **S1** | **t統計量がイベント日クラスタリング未補正**。`tStat = mean/(sd/√n)` の i.i.d. 仮定。同じ日に30件シグナルが出れば相関しており、実効nははるかに小さい。t値が最大 √30 ≈ 5.5倍に膨らむ | **偽のエッジを生む**。最も危険 | 未対応 |
| **S2** | **サバイバーシップバイアス**。`/listed/info` は現在の上場銘柄のみ。決算ギャップ後に倒産・上場廃止した企業が消え、回復率が実態より良く見える | 系統的な過大評価 | 未対応 |
| **S3** | **ストップがギャップを突き抜けた場合の約定価格**。`resolveExit` は `entryPrice × (1 - stopLossBps/10000)` ちょうどで約定したことにする。実際は寄付がストップ水準を割れば寄付で約定する。**過剰反応戦略はギャップが本体**なので影響が大きい | 損失の過小評価 | 未対応 |
| **S4** | **ストップ安・売買停止で買えない**。大幅下落の翌日は寄らないことがあるが `next_open` で平然と約定させる。**一番おいしい事例だけ実際には買えない** | 系統的な過大評価 | 未対応 |
| **S5** | **交絡イベント未除外**。決算と同日の増資・M&A・下方修正を弾いていない。「決算への過剰反応」ではなく「増資への正当な反応」を拾う | 因果の誤帰属 | 未対応 |
| **S6** | **`trials` が申告制**。`falseDiscoveryGuard(tStat, trials)` の試行回数を呼び出し側が正直に申告する前提。閾値を10通り試しても誰も強制しない。fixture の `trials: 1` を書いたのは私 | 過剰適合の温床 | 未対応 |
| **S7** | **配当が消える**。未調整価格の終値比なのでインカムが計上されない。権利落ち日に小さな偽ギャップも出る（-35%ガードでは捕まらない）。**権利落ちEdgeには致命的** | ロングの過小評価 | 未対応 |
| **S8** | **サイズ・業種中立化なし**。benchmark が 1306 のみ。小型株が系統的に違う動きなら拾うのは size factor | エッジの誤認 | 未対応 |
| **S9** | **ユニバース選定の look-ahead**。「流動性上位500」を今日の流動性で選ぶと未来情報 | 過大評価 | 未対応 |
| **S10** | **同時保有・資金制約なし**。全シグナルに同じ notional を独立に当てる。同日に30件出たら30倍の資金が要る | 実装不能な結果 | 未対応 |
| **S11** | **provider が `adjustmentFactor` を捨てる**。常に 1 で上書き。分割を検出できない。今日入れたのは保険（単日-35%以下を落とす）のみ | 偽シグナル | 保険のみ |
| **S12** | **単元株(100株)制約なし**。`notionalJpy` を連続量として扱う。5,000円の株は1単元50万円で、小口座では分散できない | 実装可能性の誤認 | 未対応 |
| **S13** | **税金なし**。譲渡益約20.315%。符号は変わらないが「やる価値があるか」は変わる | 判断材料の欠落 | 未対応 |
| **S14** | **レジーム依存を見ない**。強気相場だけで効くエッジを見分けられない | 将来の破綻 | 未対応 |
| **S15** | **FDR Guard の表示**。`t = -4.02` の負に有意な結果に `PASS` と出る。両側検定として正しいが損失Edgeが合格に読める | 誤読 | 未対応 |

### 2-2. 運用の欠陥

| # | 欠陥 | 状態 |
| --- | --- | --- |
| **B1** | **J-Quants 403 Forbidden**。`/v2/listed/info` 応答0.23秒。キー無効・期限切れ・権限不足。全銘柄 `timeout 15000ms`（ログ563回）→ 今朝のレポートは全項目0件 | **要人間** |
| **B2** | **TDnet 行スキップバグ**。2026-09-08 が `TDnet row has no disclosure document link` で落ちる（再現性あり。09-09=127件、09-07=155件は正常）。リンクなし行1つで**その日の全件が失われる** | 未対応 |
| **B3** | **launchd が古い作業コピーを実行**。pull 前は main から1555 commits 遅れ。`TDnet 404`(62回) と `mapfile: command not found` はどちらも修正済みバグの再現だった | 未対応 |
| **B4** | **CI未接続の verify 20本**。全部PASS・合計約1.3秒。`ci-pipeline-smoke.sh` へ追加するだけ | 未対応 |
| **B5** | **PR #1834 / #1836 放置**。MERGEABLE / CLEAN / check緑・2026-09-04から | 未対応 |
| **B6** | **daily の degraded 検知なし**。「取得0件」と「候補0件」が区別できない | 未対応 |
| **B7** | **`tests/research/schema.test.ts` が macOS で必ず失敗**。`/var` → `/private/var` の symlink を catalog validator が祖先symlinkとして拒否。CI(Linux)では通るため気づきにくいが、**ローカルで `pnpm research:check` が完走できない** | 未対応 |
| **B8** | price store の配置不整合。README は `securities/<market>/<code>.jsonl`、CLI は `research/prices/jquants-free/{CODE}.jsonl` | 未対応 |
| **B9** | remote branch 2023本。`feat/decision-firewall-v1`（PR #43 CLOSED / DO NOT MERGE明記）削除可 | 未対応 |

### 2-3. 未実装の接続部

| # | 欠落 | 内容 |
| --- | --- | --- |
| **M1** | Universe list | `/listed/info` 未実装。全上場銘柄コードを取得できない |
| **M2** | Bulk fetch | 価格CLIは `--code` 1件ずつ。ユニバースを回す層がない |
| **M5** | Benchmark series | 1306 を `seriesKind: "benchmark"` で保存する経路 |

（M3 signal-generator と M4 bundle-builder は 2026-09-10 に実装済み）

---

## 3. Edge カタログ

**ユーザー提案の3案はすべて含む**（B1 / B2 / C1）。既存 Edge 定義がある場合は流用する。

### A. 決算系

| ID | Edge | 2年間の供給量 | データ | 備考 |
| --- | --- | --- | --- | --- |
| **A1** | **決算ギャップ過剰反応**（急落だが通期計画は据え置き以上） | 約4,000 | `/fins/summary` | **実装済み**。配管のテストケース |
| A2 | 決算後ドリフト（上方サプライズの継続） | 約4,000 | 同上 | A1 の裏返し |
| A3 | 通期進捗率と株価反応の乖離 | 約4,000 | 同上 | 進捗率の計算が要る |

### B. 不祥事・事件系 ← **ユーザー提案②③**

既存 Edge `research/edge_registry/edges/misconduct-overreaction-recovery.yml` が
`incidentClasses` としてすべて定義済み。新規に作らず、これにサンプルを入れる。

| ID | Edge | 既存定義の該当 | 供給量 |
| --- | --- | --- | --- |
| **B1** | **業務と無関係な不祥事による下落 → 回復**（サンリオ型） | `brand_scandal` / `external_incident_venue_only` | 年数十件 |
| **B2** | **バイトテロ・従業員不祥事**（業績影響ほぼゼロ） | `employee_misconduct` | 年十数件 |
| B3 | **子会社限定の問題が親会社を過剰に下げる** | `subsidiary_localized_issue` | 年十数件 |
| B4 | 第三者委員会報告・行政処分の「出尽くし」二次反応 | 同Edge phase3 | 年数十件 |
| B5 | 役員の私的不祥事 | `executive_personal_misconduct` | 年十数件 |

**最大の障害は供給量ではなく過去データ**。TDnet が1ヶ月しか遡れないので、
**F1（価格逆引き）を先に作らないとサンプルが永久に集まらない**。

### C. 関連銘柄伝播 ← **ユーザー提案①**

既存 Edge に無い。**新規 Edge**。1イベントから N サンプル取れるので、
供給が細い B 系の唯一の増やし方。

| ID | Edge | 内容 | 供給量 |
| --- | --- | --- | --- |
| **C1** | **read-across 過剰反応** | A社の不祥事で同業B社も下落。B社に実害ゼロなら回復 | B系 × 同業社数 |
| **C2** | **親子・子会社伝播**（キオクシア系） | 子会社/関連会社のニュースで親会社が過剰反応、またはその逆 | 年数十件 |
| C3 | サプライチェーン伝播 | 顧客企業のニュースで部品企業が動く | 年数十件 |

`config/company-network.yml` に peers 構造が既にある（サンリオ→任天堂/バンナム/OLC）。
`security-master.ts` と接続して自動生成する。

### D. 需給・カレンダー系 ← **私の推し。日程が事前確定なので PIT が完璧**

| ID | Edge | 供給量 | 既存資産 |
| --- | --- | --- | --- |
| **D1** | **ロックアップ解除**前後の需給 | 年数十件 | `extract-lockup-events.ts` |
| D2 | 立会外分売 | 年百件超 | — |
| D3 | 公募増資後のリバウンド | 年数十件 | — |
| D4 | 指数入替（TOPIX/日経225） | 年数十件 | — |
| **D5** | **権利落ち過剰反応** | 1,000〜2,000 | 既存Edge `ex-rights-overreaction-recovery` |
| D6 | IPO セカンダリ | 年百件程度 | `sync-ipo.ts` |

**D5 は S7（配当が消える）を直さないと測定不能。**

### E. イベントドリブン

| ID | Edge | 備考 |
| --- | --- | --- |
| E1 | TOB / 完全子会社化のスプレッド | 裁定が速い。個人には厳しい可能性 |
| E2 | 自社株買い発表後 | 発表と実施の乖離を見る |

### F. イベント供給の基盤

| ID | 内容 |
| --- | --- |
| **F1** | **価格逆引きイベント検出**。過去5年(Freeは2年)の全銘柄から「benchmark比 -8%以上」を抽出 → 決算日・増資・M&Aを機械的に除外 → 残りに原因ラベル。**B系・C系の前提条件** |

F1 は選択バイアス（下がったケースだけ集める）を持つが、
エントリー条件自体が「大きく下げたら」なので条件付き母集団としては整合する。
ただし「不祥事があったが下がらなかった」ケースを control として別途持つ。

---

## 4. 実装ロードマップ（推奨順）

### 原則

**測定の正しさを、エッジ探索より先に置く。**
S1〜S6 を入れる前に出した数字は全部やり直しになる。後から入れるほど損失が大きい。

とくに **S6（試行回数台帳）は、最初のパラメータ探索を始める前**に必要。
一度でも「閾値を -7 から -8 に変えてみた」をやったら、記録されない試行が発生する。

---

### Phase 0 — 人間の作業（並行）

- **B1 J-Quants キー復旧**。再発行 or `JQUANTS_EMAIL`/`PASSWORD` 設定
- 復旧後に**要確認**: Free で実際に取得できる履歴の開始日、`/listed/info` の可用性

### Phase 1 — 自分を騙さない仕組み（J-Quants 不要・今すぐ）

| 順 | 項目 | 内容 |
| --- | --- | --- |
| 1 | **S6 試行回数台帳** | `research/trials.jsonl` に追記専用で「いつ・どのEdgeで・どのパラメータを試したか」を記録。`trials` を自動算出。検証前に事前登録し hash を残す。**閾値をいじって黒字にする誘惑を構造的に不可能にする** |
| 2 | **S1 クラスタリング補正** | イベント日でクラスタ化し、日次平均のt統計量を使う。`aggregate` に `clusterKeys` を追加。**偽のエッジを最も生みやすい穴** |
| 3 | **S15 FDR 表示** | 符号を併記。負に有意なら `SIGNIFICANT_NEGATIVE` |
| 4 | **S3 ストップのギャップ約定** | ロングは `min(stopLevel, bar.open)` で約定。ショートは `max` |
| 5 | **S13 税金 / S12 単元株** | CostModel に税率、`notionalJpy` を単元株に丸める |

**完了条件**: 合成データで、クラスタ化した t統計量と試行回数補正が効くこと。

### Phase 2 — データの正しさ（J-Quants 復旧後）

| 順 | 項目 | 内容 |
| --- | --- | --- |
| 6 | **M1 + S2 + S9 PIT ユニバース** | `/listed/info` を日次スナップショットで保存。上場廃止銘柄を含む PIT ユニバースを復元可能にする。`security-master.ts` へ接続。流動性フィルタも過去時点の流動性で行う |
| 7 | **S11 adjustmentFactor 保存** | provider が捨てている実値を record に入れる。分割を正しく検出 |
| 8 | **S7 配当** | 配当データを price store へ。トータルリターンで測る。**D5 権利落ちEdgeの前提** |
| 9 | **M5 benchmark** | 1306 を benchmark として保存。TOPIX との乖離を記録 |
| 10 | **M2 bulk fetch** | ユニバースを回して2年分を取得（約25分）。`validate-prices.ts` で全件検証 |
| 11 | **S4 執行可能性ゲート** | ストップ安・売買停止・寄らずを検出。`SkipReason` に `limit_move_no_execution` |
| 12 | **S5 交絡スキャン** | 反応日±1日の増資・M&A・下方修正で除外 |

**完了条件**: 実データが price store に入り、全件が検証を通る。

### Phase 3 — 最初のエッジ検証

| 順 | 項目 |
| --- | --- |
| 13 | **A1 決算ギャップ**を実データで回す。n≥120、手数料・税込みの Net Alpha |
| 14 | **S8 / S14 分解** — 時価総額帯別・業種別・年別に分けて報告 |
| 15 | **S10 資金制約** — 同時保有上限を入れたポートフォリオ版の再計算 |
| 16 | walk-forward（探索期間 / confirmatory 期間の分離） |

**判定**
- **プラス** → Phase 4 へ
- **マイナス** → A1 を捨てる。**閾値をいじらない**。D 系へ移る
- **数字が出ない** → 配管が壊れている。Edge を増やさず配管を直す

**holdout vault（2025-07〜2026-06）は最後の1回まで開けない。**

### Phase 4 — エッジを広げる

| 順 | 項目 |
| --- | --- |
| 17 | **D1 ロックアップ解除 / D5 権利落ち** — 日程が事前確定で PIT が完璧。既存資産あり |
| 18 | **F1 価格逆引きイベント検出** — B系・C系のサンプル供給を作る |
| 19 | **C1 / C2 read-across**（キオクシア系）— `company-network.yml` × `security-master` |
| 20 | **B1 / B2 / B3 不祥事系**（サンリオ型・バイトテロ・子会社）— 既存 Edge 定義にサンプルを入れる |
| 21 | D2 / D3 / D4 / A2 / E2 を同じ配管で追加 |

**新しい Edge を足すコストが「イベントリストを作るだけ」になっている状態**を目指す。

### Phase 5 — 実弾（Phase 3-4 で期待値プラスが出た場合のみ）

| 順 | 項目 |
| --- | --- |
| 22 | **リアルタイム価格源**。J-Quants Free の84日遅延では紙トレードすらできない。証券会社API or 有料プラン |
| 23 | **ポジションサイジング**。1トレードのリスク = 口座の r%、株数 = リスク額 ÷ ストップ幅 |
| 24 | **紙トレード帳簿**。backtest 想定と実約定の乖離(slippage)を継続測定。**これがないと backtest がいつ壊れたか分からない** |
| 25 | 通知をシグナル形式へ（銘柄/方向/entry/stop/target/期限/反証条件/過去実績） |
| 26 | `safe-output-audit` をオーナー向けと public で分離 |

### Phase 6 — 運用の穴（Phase 1 と並行して随時）

B2 TDnet行スキップ → B4 CI接続 → B5 PR merge → B3 launchd → B6 degraded →
B7 macOSテスト → B8 配置 → B9 ブランチ整理

---

## 5. 凍結するもの

Phase 3 で数字が出るまで触らない（消しもしない）。

- market-event の provenance / canonicality 強化 → B4 完了時点で凍結
- 新規 Market Event collector
- Owner UI v2 の残り14 route
- Storage Foundation v1 Phase 0 の新規実装（棚卸しのみ）
- `src/research/` の EDINET-Sanrio 系 CLI 28本

---

## 6. KPI

| 指標 | 2026-09-10 | 目標 |
| --- | --- | --- |
| 確定 outcome | **0件** | 300+ |
| `expectedDirection: unknown` 率 | **100%** (65/65) | 0% |
| `BacktestSignal` 生成器 | 実装済み | 実データ接続 |
| 合成 end-to-end | 通過 | 維持 |
| price store のレコード | **0件** | 500銘柄 × 2年 |
| 実データ backtest | **0回** | n≥120 |
| クラスタ補正後の t 統計量 | **未算出** | 算出済み |
| 試行回数台帳 | **無し** | 全試行を記録 |
| 手数料・税込み Net Alpha | **未算出** | 符号が判明 |
| 監視母集団 | **31銘柄** | 500+（PIT） |

---

## 7. 維持する原則

- 未確定日時を捏造しない
- SNS・掲示板を signal source にしない
- 件数が少ない学習結果で強い判断をしない
- 自動でルールを削除しない（提案のみ）
- holdout を早期に開けない
- Gross と Net を必ず分けて報告する
- 執行できない取引を執行できたことにしない
- **合成データで通ったテストを、実データの保証とみなさない**（2026-09-10 の分割バグの教訓）

変更するのは「買い推奨にしない」の1点のみ。しかもオーナー向け出力に限定する。

---

## 8. 正直に言っておくこと

- Phase 3 で全ルールの期待値がマイナスになる可能性は十分ある。そのときの正解は
  閾値をいじることではなく**やらないと決めること**
- S1（クラスタリング補正）を入れると、入れる前より t統計量は必ず下がる。
  「有意だったものが有意でなくなる」のは改悪ではなく、**それまでが嘘だった**ということ
- 過去2年しかない制約下では、レジームを跨いだ検証ができない。
  Free プランで出た結果は「直近2年でそう見えた」以上のことを主張できない
